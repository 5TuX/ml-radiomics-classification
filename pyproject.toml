[project]
name = "ml-radiomics-classification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
]

[tool.mypy]
ignore_missing_imports = true
